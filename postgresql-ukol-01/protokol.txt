drop table tab1;
DROP TABLE
create table tab1 (a integer primary key, b varchar(20));
CREATE TABLE
CREATE OR REPLACE FUNCTION insert_into_tab1(kolik INTEGER) RETURNS integer  AS $$
BEGIN
  for i in 1..kolik loop
    insert into tab1 values (i,'aaa');
  end loop;
  RETURN kolik;
END;
$$ LANGUAGE plpgsql ;
CREATE FUNCTION
select insert_into_tab1(20);
 insert_into_tab1 
------------------
               20
(1 row)

select count(*) from tab1;
 count 
-------
    20
(1 row)

